import React from 'react';

const useSubscribed = () => {
    return (
        <div>

        </div>
    );
};

export default useSubscribed;



// Todo: 1. Create a new component called useSubscribed.jsx to check the users are subscribed or not.